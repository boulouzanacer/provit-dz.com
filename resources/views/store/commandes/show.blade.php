@extends('store.layout')
@section('content')
<div class="grid grid-cols-1 xl:grid-cols-3 gap-6"><div class="rounded-3xl border border-slate-200 bg-white p-6"><div class="text-xl font-extrabold">Commande #{{ $order->id }}</div><div class="mt-3 text-sm text-slate-500">Distributeur: {{ $order->fournisseur?->nom_frs }}</div><div class="mt-1 text-sm text-slate-500">Statut: {{ $order->statut }}</div><div class="mt-1 text-sm text-slate-500">Date: {{ optional($order->date_cmd)->format('d/m/Y H:i') }}</div><div class="mt-1 text-sm text-slate-500">Adresse: {{ $order->adresse_livraison }}</div></div><div class="rounded-3xl border border-slate-200 bg-white p-6 xl:col-span-2 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-slate-500"><tr><th class="py-3 pr-4 text-left">Produit</th><th class="py-3 pr-4 text-left">Qte</th><th class="py-3 pr-4 text-left">PU</th><th class="py-3 text-right">Total</th></tr></thead><tbody class="divide-y divide-slate-200">@foreach($order->lignes as $line)<tr><td class="py-3 pr-4">{{ $line->produit?->designation }}</td><td class="py-3 pr-4">{{ $line->quantite }}</td><td class="py-3 pr-4">{{ number_format($line->prix_unitaire, 2, '.', ' ') }} DA</td><td class="py-3 text-right">{{ number_format($line->sous_total, 2, '.', ' ') }} DA</td></tr>@endforeach</tbody></table></div></div>
@endsection
