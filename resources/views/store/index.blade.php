@extends('store.layout')
@section('content')
<div class="space-y-6"><div class="rounded-3xl border border-slate-200 bg-white p-6"><div class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div><div class="text-3xl font-extrabold tracking-wide">Catalogue Pro-Vit</div><div class="mt-1 text-sm text-slate-500">Affichage des produits selon le distributeur selectionne.</div></div><div class="flex gap-3"><div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><div class="text-xs text-slate-500">Panier</div><div class="font-extrabold">{{ count($cartSummary['items']) }} article(s)</div></div><div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><div class="text-xs text-slate-500">Total</div><div class="font-extrabold">{{ number_format($cartSummary['total'], 2, '.', ' ') }} DA</div></div></div></div><form method="GET" action="{{ url('/') }}" class="mt-6 grid grid-cols-1 md:grid-cols-3 gap-3"><div class="md:col-span-2"><input name="q" value="{{ $q }}" placeholder="Recherche produit ou code barre..." class="w-full rounded-2xl border border-slate-200 px-4 py-3"></div><div><select name="category" onchange="this.form.submit()" class="w-full rounded-2xl border border-slate-200 px-4 py-3"><option value="0">Toutes categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected($selectedCategory == $category->id)>{{ $category->nom }}</option>@endforeach</select></div></form></div><div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">@forelse($products as $product)<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><a href="{{ url('/produits/'.$product->id) }}" class="block aspect-square bg-slate-100">@if($product->image_principale)<img src="{{ $product->image_principale }}" class="h-full w-full object-cover" alt="">@else<div class="flex h-full items-center justify-center text-slate-400"><i class="fa-regular fa-image text-3xl"></i></div>@endif</a><div class="p-4"><div class="font-extrabold leading-tight">{{ $product->designation }}</div><div class="mt-1 text-xs text-slate-500">{{ $product->category?->nom ?: 'Sans categorie' }}</div><div class="mt-3 flex items-center justify-between"><div><div class="text-sm font-extrabold">{{ number_format($product->prixUnitairePourQuantite($client ?? null, 1), 2, '.', ' ') }} DA</div><div class="text-xs {{ $product->stockForDistributor($selectedFrsId) > 0 ? 'text-emerald-700' : 'text-red-600' }}">Stock: {{ $product->stockForDistributor($selectedFrsId) }}</div></div><form method="POST" action="{{ url('/panier/add') }}">@csrf<input type="hidden" name="produit_id" value="{{ $product->id }}"><input type="hidden" name="qty" value="1"><input type="hidden" name="id_frs" value="{{ $selectedFrsId }}"><button class="inline-flex h-10 w-10 items-center justify-center rounded-2xl text-white {{ $product->stockForDistributor($selectedFrsId) <= 0 ? 'opacity-40' : '' }}" style="background:linear-gradient(135deg,#1E6FD9,#0f3b8c)" @disabled($product->stockForDistributor($selectedFrsId) <= 0)><i class="fa-solid fa-cart-plus"></i></button></form></div></div></div>@empty<div class="col-span-full rounded-3xl border border-slate-200 bg-white p-10 text-center text-slate-500">Aucun produit disponible.</div>@endforelse</div><div>{{ $products->links() }}</div></div>
@endsection
