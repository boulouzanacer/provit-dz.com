@extends('store.layout')
@section('content')
<div class="grid grid-cols-1 xl:grid-cols-3 gap-6"><div class="xl:col-span-2 rounded-3xl border border-slate-200 bg-white p-6 overflow-x-auto"><div class="mb-4 flex items-center justify-between"><div class="text-xl font-extrabold">Mon panier</div><form method="POST" action="{{ url('/panier/clear') }}" data-confirm-delete data-confirm-title="Vider le panier ?" data-confirm-message="Tous les produits ajoutes au panier seront retires de cette commande en preparation." data-confirm-button="Oui, vider le panier">@csrf<button class="rounded-2xl border border-slate-200 px-4 py-2 text-sm font-semibold hover:bg-slate-50">Vider</button></form></div><table class="min-w-full text-sm"><thead class="text-slate-500"><tr><th class="py-3 pr-4 text-left">Produit</th><th class="py-3 pr-4 text-left">PU</th><th class="py-3 pr-4 text-left">Quantite</th><th class="py-3 pr-4 text-left">Total</th><th class="py-3 text-right">Action</th></tr></thead><tbody class="divide-y divide-slate-200">@forelse($summary['items'] as $item)<tr><td class="py-3 pr-4 font-bold">{{ $item['produit']->designation }}</td><td class="py-3 pr-4">{{ number_format($item['unit'], 2, '.', ' ') }} DA</td><td class="py-3 pr-4"><form method="POST" action="{{ url('/panier/update') }}" class="flex items-center gap-2">@csrf<input type="hidden" name="produit_id" value="{{ $item['produit']->id }}"><input type="number" name="qty" min="1" max="{{ $item['stock'] }}" value="{{ $item['qty'] }}" class="w-24 rounded-2xl border border-slate-200 px-3 py-2"><button class="h-9 w-9 rounded-xl border border-slate-200 hover:bg-slate-50"><i class="fa-solid fa-check"></i></button></form></td><td class="py-3 pr-4">{{ number_format($item['total'], 2, '.', ' ') }} DA</td><td class="py-3 text-right"><form method="POST" action="{{ url('/panier/remove') }}" data-confirm-delete data-confirm-title="Retirer ce produit du panier ?" data-confirm-message="Le produit {{ $item['produit']->designation }} sera retire de votre panier actuel." data-confirm-button="Oui, retirer le produit">@csrf<input type="hidden" name="produit_id" value="{{ $item['produit']->id }}"><button class="h-9 w-9 rounded-xl border border-slate-200 text-red-600 hover:bg-red-50"><i class="fa-solid fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="py-8 text-center text-slate-500">Votre panier est vide.</td></tr>@endforelse</tbody></table></div><div class="rounded-3xl border border-slate-200 bg-white p-6"><div class="text-xl font-extrabold">Resume</div><div class="mt-4 space-y-3 text-sm"><div class="flex items-center justify-between"><span>Distributeur</span><span class="font-bold">{{ $summary['distributor']?->nom_frs ?: '?' }}</span></div><div class="flex items-center justify-between"><span>Total</span><span class="text-xl font-extrabold">{{ number_format($summary['total'], 2, '.', ' ') }} DA</span></div></div><a href="{{ url('/checkout') }}" class="mt-6 inline-flex w-full items-center justify-center rounded-2xl px-4 py-3 text-sm font-extrabold text-white {{ count($summary['items']) === 0 ? 'pointer-events-none opacity-40' : '' }}" style="background:linear-gradient(135deg,#1E6FD9,#0f3b8c)">Passer commande</a></div></div>
@endsection
