@extends('layouts.fournisseur')
@section('content')
<div class="space-y-4"><div class="rounded-3xl border border-white/10 bg-[var(--panel-card)] p-5"><form class="max-w-md"><input name="q" value="{{ $q }}" placeholder="Rechercher un produit..." class="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"></form></div><div class="rounded-3xl border border-white/10 bg-[var(--panel-card)] p-5 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-white/60"><tr><th class="py-3 pr-4 text-left">Produit</th><th class="py-3 pr-4 text-left">Categorie</th><th class="py-3 pr-4 text-left">Prix</th><th class="py-3 pr-4 text-left">Stock actuel</th><th class="py-3 text-left">Etat</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($products as $product)<tr><td class="py-3 pr-4"><div class="font-bold">{{ $product->designation }}</div><div class="text-xs text-white/60">{{ $product->code_barre ?: 'Sans code barre' }}</div></td><td class="py-3 pr-4">{{ $product->category?->nom ?: '?' }}</td><td class="py-3 pr-4">{{ number_format($product->pv_1, 2, '.', ' ') }} DA</td><td class="py-3 pr-4">{{ $product->stockForDistributor($frsId) }}</td><td class="py-3 pr-4"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $product->stockForDistributor($frsId) > 0 ? 'bg-emerald-500/15 text-emerald-200' : 'bg-amber-500/15 text-amber-200' }}">{{ $product->stockForDistributor($frsId) > 0 ? 'Disponible' : 'A zero' }}</span></td></tr>@empty<tr><td colspan="5" class="py-8 text-center text-white/60">Aucun produit</td></tr>@endforelse</tbody></table></div><div>{{ $products->links() }}</div></div>
@endsection
