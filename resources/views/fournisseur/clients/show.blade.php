@extends('layouts.fournisseur')
@section('content')
<div class="grid grid-cols-1 xl:grid-cols-3 gap-4"><div class="rounded-3xl border border-white/10 bg-[var(--panel-card)] p-5 space-y-3"><div class="text-xl font-extrabold">{{ $client->prenom }} {{ $client->nom }}</div><div class="text-sm text-white/70">{{ $client->email }}</div><div class="text-sm text-white/70">{{ $client->telephone ?: '?' }}</div><div class="text-sm text-white/70">{{ $client->adresse ?: '?' }}</div></div><div class="xl:col-span-2 rounded-3xl border border-white/10 bg-[var(--panel-card)] p-5 overflow-x-auto"><div class="mb-4 font-extrabold">Commandes</div><table class="min-w-full text-sm"><thead class="text-white/60"><tr><th class="py-3 pr-4 text-left">#</th><th class="py-3 pr-4 text-left">Date</th><th class="py-3 pr-4 text-left">Statut</th><th class="py-3 text-right">Montant</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($client->commandes as $order)<tr><td class="py-3 pr-4"><a href="{{ url('/distributeur/commandes/'.$order->id) }}">#{{ $order->id }}</a></td><td class="py-3 pr-4">{{ optional($order->date_cmd)->format('d/m/Y H:i') }}</td><td class="py-3 pr-4">{{ $order->statut }}</td><td class="py-3 text-right">{{ number_format($order->montant_total, 2, '.', ' ') }} DA</td></tr>@empty<tr><td colspan="4" class="py-8 text-center text-white/60">Aucune commande</td></tr>@endforelse</tbody></table></div></div>
@endsection
