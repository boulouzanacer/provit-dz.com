@extends('layouts.admin')
@section('content')
<div class="space-y-4"><div class="rounded-3xl border border-white/10 bg-[var(--admin-card)] p-5"><form class="max-w-md"><input name="q" value="{{ $q }}" placeholder="Rechercher un client..." class="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3"></form></div><div class="rounded-3xl border border-white/10 bg-[var(--admin-card)] p-5 overflow-x-auto"><table class="min-w-full text-sm"><thead class="text-white/60"><tr><th class="py-3 pr-4 text-left">Client</th><th class="py-3 pr-4 text-left">Distributeur</th><th class="py-3 pr-4 text-left">Telephone</th><th class="py-3 pr-4 text-left">Code</th><th class="py-3 text-right">Actions</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($clients as $client)<tr><td class="py-3 pr-4"><div class="font-bold">{{ $client->prenom }} {{ $client->nom }}</div><div class="text-xs text-white/60">{{ $client->email }}</div></td><td class="py-3 pr-4">{{ $client->fournisseur?->nom_frs ?: '?' }}</td><td class="py-3 pr-4">{{ $client->telephone ?: '?' }}</td><td class="py-3 pr-4">{{ $client->code_client ?: '?' }}</td><td class="py-3 text-right"><div class="inline-flex items-center gap-2"><a href="{{ url('/admin/clients/'.$client->id) }}" class="h-9 w-9 rounded-xl border border-white/10 flex items-center justify-center hover:bg-white/10"><i class="fa-solid fa-eye"></i></a><form method="POST" action="{{ url('/admin/clients/'.$client->id) }}" onsubmit="return confirm('Supprimer ce client ?')">@csrf @method('DELETE')<button class="h-9 w-9 rounded-xl border border-white/10 hover:bg-white/10 text-red-300"><i class="fa-solid fa-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="5" class="py-8 text-center text-white/60">Aucun client</td></tr>@endforelse</tbody></table></div><div>{{ $clients->links() }}</div></div>
@endsection
